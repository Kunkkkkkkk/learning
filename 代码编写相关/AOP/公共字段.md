**`execution(\* com.sky.mapper.\*.(..))`**：

- **`execution(\* ... )`**：`execution` 是用于匹配方法执行的表达式。
- **`\*`**：表示匹配任意返回类型的所有方法。
- **`com.sky.mapper.\*`**：表示 `com.sky.mapper` 包下的所有类。
- **`.\*(..)`**：表示匹配任意方法名（`.` 匹配方法名，`(..)` 匹配任意参数）。







要知道，即使写了@AutoFill，也要在sqlxml里面写上creattime这些，从原理理解