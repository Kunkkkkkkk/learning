```sql
1. `ABS(x)`
2. `SIGN(X)`
3. `PI()`
4. `CEIL(x), CEILING(x)`  大于等于x的最小整数
5. `FLOOR(x)`			小于等于x的最大整数
6. `LEAST(e1,e2,e3...)`
7. `GREATEST(e1,e2,e3...)`
8. `MOD(x,y)`
9. `RAND()`
10. `RAND(x)`
11. `ROUND(x)`
12. `ROUND(x,y)`			四舍五入，再截取到小数后y位
13. `TRUNCATE(x,y)`  直接截取到小数后y位
14. `SQRT(x)`
15.	`DATADIFF(DATA1,DATA2)`  data1-data2，两个日期间的日子,经常用来给你生日算年龄
```

```
1. `AVG()`
2. `SUM()`
3. `MIN()`
4. `MAX()`
```

```
char_length和length的区别

- char_length(str)

1. 计算单位：字符
2. 不管汉字还是数字或者是字母都算是一个字符

- length(str)

1. 计算单位：字节
2. utf8编码：一个汉字三个字节，一个数字或字母一个字节。
3. gbk编码：一个汉字两个字节，一个数字或字母一个字节。
```



统计字符串长度时可以用replace(字符串,' ','')来去除空格
